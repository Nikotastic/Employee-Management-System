# =============================================================================
# TalentoPlus S.A.S - Employee Management System
# Environment Variables Configuration Template
# =============================================================================
# INSTRUCCIONES:
# 1. Copie este archivo y renómbrelo a ".env"
# 2. Reemplace los valores de ejemplo con sus credenciales reales
# 3. NUNCA versione el archivo .env en Git (está en .gitignore)
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION (PostgreSQL)
# -----------------------------------------------------------------------------
POSTGRES_DB=TalentoPlusDB
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
PG_PORT=5432

# -----------------------------------------------------------------------------
# API CONFIGURATION
# -----------------------------------------------------------------------------
API_PORT=5000

# -----------------------------------------------------------------------------
# WEB CONFIGURATION
# -----------------------------------------------------------------------------
WEB_PORT=80

# -----------------------------------------------------------------------------
# JWT CONFIGURATION
# -----------------------------------------------------------------------------
# Genere una clave segura de al menos 64 caracteres
# Puede usar: openssl rand -base64 64
JWT__Key=your_jwt_secret_key_here_minimum_64_characters
JWT__Issuer=TalentoPlusAPI
JWT__Audience=TalentoPlusClient
JWT__ExpiryMinutes=15

# -----------------------------------------------------------------------------
# PGADMIN CONFIGURATION
# -----------------------------------------------------------------------------
PGADMIN_EMAIL=admin@talentoplus.com
PGADMIN_PASSWORD=your_pgadmin_password_here
PGADMIN_PORT=8080

# -----------------------------------------------------------------------------
# ASP.NET CORE ENVIRONMENT
# -----------------------------------------------------------------------------
# Valores posibles: Development, Staging, Production
ASPNETCORE_ENVIRONMENT=Development

# -----------------------------------------------------------------------------
# EMAIL CONFIGURATION (Gmail SMTP)
# -----------------------------------------------------------------------------
# Nota: Para Gmail, necesitas generar una "App Password" en tu cuenta
# https://myaccount.google.com/apppasswords
EmailSettings__SmtpServer=smtp.gmail.com
EmailSettings__SmtpPort=587
EmailSettings__SenderEmail=your_email@gmail.com
EmailSettings__SenderName=TalentoPlus S.A.S
EmailSettings__Username=your_email@gmail.com
EmailSettings__Password=your_gmail_app_password_here
EmailSettings__EnableSsl=true
EmailSettings__TimeoutSeconds=30

# -----------------------------------------------------------------------------
# AI CONFIGURATION (Gemini)
# -----------------------------------------------------------------------------
# Obtén tu API Key en: https://makersuite.google.com/app/apikey
AISettings__Provider=Gemini
AISettings__ApiKey=your_gemini_api_key_here
AISettings__Model=gemini-2.0-flash

# -----------------------------------------------------------------------------
# CONNECTION STRING (Auto-generated for Docker)
# -----------------------------------------------------------------------------
# Esta cadena se genera automáticamente para Docker usando las variables anteriores
ConnectionStrings__PostgresConnection=Host=db;Port=5432;Database=TalentoPlusDB;Username=postgres;Password=your_secure_password_here

