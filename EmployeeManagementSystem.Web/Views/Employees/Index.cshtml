@model IEnumerable<EmployeeDto>
@{
    ViewData["Title"] = "Gestión de Empleados";
}

<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-md-6">
            <h1 class="h3">
                <i class="fas fa-users"></i> Gestión de Empleados
            </h1>
            <p class="text-muted">Administrar información de empleados</p>
        </div>
        <div class="col-md-6 text-end">
            <a asp-action="Create" class="btn btn-primary">
                <i class="fas fa-plus"></i> Nuevo Empleado
            </a>
            <a asp-action="ImportExcel" class="btn btn-success">
                <i class="fas fa-file-excel"></i> Importar Excel
            </a>
        </div>
    </div>

    <div class="card shadow">
        <div class="card-body">
            @if (Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-hover table-striped" id="employeesTable">
                        <thead class="table-dark">
                            <tr>
                                <th>Documento</th>
                                <th>Nombre Completo</th>
                                <th>Email</th>
                                <th>Teléfono</th>
                                <th>Cargo</th>
                                <th>Departamento</th>
                                <th>Estado</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var employee in Model)
                            {
                                <tr>
                                    <td>@employee.Document</td>
                                    <td>@employee.FirstName @employee.LastName</td>
                                    <td>@employee.Email</td>
                                    <td>@employee.Phone</td>
                                    <td>@employee.JobPositionName</td>
                                    <td>@employee.DepartmentName</td>
                                    <td>
                                        @if (employee.Status == "Active")
                                        {
                                            <span class="badge bg-success">Activo</span>
                                        }
                                        else if (employee.Status == "Vacation")
                                        {
                                            <span class="badge bg-info">Vacaciones</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">Inactivo</span>
                                        }
                                    </td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <a asp-action="Details" asp-route-id="@employee.Id" class="btn btn-sm btn-info" title="Ver detalles">
                                                <i class="fas fa-eye"></i>
                                            </a>
                                            <a asp-action="Edit" asp-route-id="@employee.Id" class="btn btn-sm btn-warning" title="Editar">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                            <a asp-action="DownloadCV" asp-route-id="@employee.Id" class="btn btn-sm btn-success" title="Descargar CV">
                                                <i class="fas fa-file-pdf"></i>
                                            </a>
                                            <a asp-action="Delete" asp-route-id="@employee.Id" class="btn btn-sm btn-danger" title="Eliminar">
                                                <i class="fas fa-trash"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="alert alert-info">
                    <i class="fas fa-info-circle"></i> No hay empleados registrados. 
                    <a asp-action="Create" class="alert-link">Crear el primero</a> o 
                    <a asp-action="ImportExcel" class="alert-link">importar desde Excel</a>.
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Búsqueda simple en la tabla
        $(document).ready(function() {
            // Agregar campo de búsqueda
            if ($('#employeesTable tbody tr').length > 0) {
                $('#employeesTable').before(`
                    <div class="mb-3">
                        <input type="text" id="searchBox" class="form-control" placeholder="Buscar empleado...">
                    </div>
                `);

                $('#searchBox').on('keyup', function() {
                    var value = $(this).val().toLowerCase();
                    $('#employeesTable tbody tr').filter(function() {
                        $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1);
                    });
                });
            }
        });
    </script>
}

